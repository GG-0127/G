name: Daily News Update

on:
  schedule:
    # 每天北京时间 08:00 (UTC 00:00) 运行
    - cron: "0 0 * * *"
  workflow_dispatch:

jobs:
  update-news:
    runs-on: ubuntu-latest
    permissions:
      contents: write

    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          token: ${{ secrets.GITHUB_TOKEN }}

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: "20"
          cache: "npm"

      - name: Install dependencies
        run: npm ci

      - name: Run news update script
        id: update
        run: npx tsx scripts/update-news.ts
        continue-on-error: true

      - name: Report status
        if: steps.update.outcome == 'failure'
        run: echo "News update script failed, but workflow continues."

      - name: Check for changes
        id: check
        run: |
          if [ -n "$(git status --porcelain src/data/news.ts)" ]; then
            echo "changed=true" >> $GITHUB_OUTPUT
            echo "News file has changes, will commit."
          else
            echo "changed=false" >> $GITHUB_OUTPUT
            echo "No changes in news file."
          fi

      - name: Commit and push
        if: steps.check.outputs.changed == 'true'
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add src/data/news.ts
          git commit -m "chore: 自动更新考公热点新闻 [skip ci]"
          git push